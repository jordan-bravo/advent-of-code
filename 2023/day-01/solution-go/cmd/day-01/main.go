package main

import "fmt"
import "unicode"

func main() {
	fmt.Println("Hello world!")
	// for each line, do the following:
	// remove all letters
	// for each character, do the following:
	// if character is number, add to new slice
	// for new slice:
	// if length is one, copy number to make two
	// if length is two, done
	// if length is > 2, remove all but first and last
	// finally:
	// sum all numbers
}
